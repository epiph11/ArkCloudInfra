# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
.terraform.lock.hcl

# Lambda deployment packages — build artefacts, rebuilt from source by the documented build
# command (see modules/aws/secret-rotation/lambda/README.md). Contains vendored binary wheels
# (psycopg2) that are platform-specific and several MB; committing them would bloat history for
# something reproducible in one command.
modules/aws/*/lambda/build/

# Never commit real credentials
*.pem
*.key
.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Sprint 6 incident (2026-08-28) : ce fichier binaire contient tfstate+tfstate-prev en clair,
# jamais un simple plan lisible — committé par erreur une fois, secrets exposes publiquement,
# purge d'historique + rotation totale requises. Ne JAMAIS le committer.
*.tfplan
tfplan
environments/*/tfplan
